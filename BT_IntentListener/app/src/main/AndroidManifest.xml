<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.huflit.bt_intentlistener">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BT_IntentListener">
        <activity android:name=".MainActivity2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FourActivity"
            android:label="@string/four_name" />
        <activity
            android:name=".ThreeActivity"
            android:label="@string/three_name"></activity>
        <activity
            android:name=".TwoActivity"
            android:label="@string/two_name" />
        <activity
            android:name=".OneActivity"
            android:label="@string/one_name">

        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>